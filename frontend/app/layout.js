export const metadata = {
  title: 'PYUSD Subscription Service',
  description: 'PayPal USD powered subscription platform',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
